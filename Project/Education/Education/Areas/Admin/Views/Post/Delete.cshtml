@model Education.Areas.Admin.Model.Post

@{
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}
@using (Html.BeginForm())
{
    @Html.ValidationSummary(true)
    @Html.AntiForgeryToken()
    <div class="content-header">
        <table cellspacing="0">
            <tbody>
                <tr>
                    <td style="width:50%;"><h3 class="icon-head head-cms-page">Xóa bài viết: @Model.Name</h3></td>
                    <td class="form-buttons">
                        <button class="scalable Back" name="submit" onclick="setLocation('/Admin/Post')" type="button" value="Back"><span>Quay lại</span></button>
                            <button class="scalable Delete" name="submit" onclick="return confirm('Bạn có muốn xóa bài viết này')" type="submit" value="Delete"><span>Xóa trang</span></button>
                    </td>
                </tr>
            </tbody>
        </table>
    </div>

    <div class="entry-edit">
        @Html.HiddenFor(model => model.ID)
        <div class="entry-edit-head">
            <h4 class="icon-head head-edit-form fieldset-legend"><a href="#">Xác nhận xóa bài viết: @Model.Name</a></h4>
            <div class="form-buttons"></div>
        </div>
        <div class="fieldset " id="user_base_fieldset">
            <div class="hor-scroll">
                <table cellspacing="0" class="form-list">
                    <tr>
                        <td class="label">
                            @Html.LabelFor(model => model.CategoryID)
                        </td>
                        <td class="value">
                            @Model.Category.Name
                        </td>
                    </tr>
                    <tr>
                        <td class="label">
                            @Html.LabelFor(model => model.Name)
                        </td>
                        <td class="value">
                            @Model.Name
                        </td>
                    </tr>
                    <tr>
                        <td class="label">
                            @Html.LabelFor(model => model.Description)
                        </td>
                        <td class="value">
                            @Model.Description
                        </td>
                    </tr>
                    <tr>
                        <td class="label">
                            @Html.LabelFor(model => model.Alias)
                        </td>
                        <td class="value">
                            @Model.Alias
                        </td>
                    </tr>
                    <tr>
                        <td class="label">@Html.LabelFor(model => model.ImageUrl)</td>
                        <td class="value">
                            @Html.HiddenFor(model => model.ImageUrl)
                            @if (!string.IsNullOrEmpty(Model.ImageUrl))
                            {
                                <img style="max-width: 200px;" id="img-path" src="@Url.Content(Model.ImageUrl)" alt="" />
                            }
                            else
                            {
                                <img style="max-width: 200px;" id="img-path" src="@Url.Content("~/Assets/Admin/Images/default.gif")" alt="" />
                            }

                            <span id="validityMessages" style="color: Red;"></span>
                            <br />@Html.ValidationMessageFor(model => model.ImageUrl)
                        </td>
                    </tr>

                </table>
            </div>
        </div>
    </div>
}